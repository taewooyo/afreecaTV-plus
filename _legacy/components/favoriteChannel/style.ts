import styled from "styled-components";

export const ChannelContainer = styled.div``;

export const ChannelImage = styled.img`
    border-radius: 7px;
    cursor: pointer;
`;

export const ChannelHashTag = styled.div`
    display: flex;
    flex-direction: row;
    margin-top: 10px;
`;